ssh

pwd / whoami / hostname

cd / ls
  list files in directories, hidden/sorted/

mkdir

-tab- discussion
-root- discussion
-/dev- discussion

-where are my programs ( /bin /usr/bin /usr/local/bin /home/$user/bin)
-$PATH / $USER / $HOSTNAME

-$PS1
\u \h \d \t
export PS1="\u@\h \w> "

echo
  printing text to the console, tie in with redirecting

Math Simple way

  echo $[4*90]

cat
    read contents of files

file
  find out what types of files we have (xxd | head)

pipe / redirect " | " and " >> "

man

mv cp rm

variables

Expansion / Sed built in

*break*

Useful Commands for Scripts:

if / then

for loop

x=$(cat X1_2_E002C008_13091345.cdl | grep [0-9])

for i in ${x/*>/}; do echo $i; done

while loop

capture input
  y=$(ls)
  x=$(read)

*break*

find / sed / grep

Range of Lines:
  sed '2,4d' example $d = lines

Insert at End of Line:
  sed 's/$/"end"/' example

find and replace:
  sed 's/erors/errors/g' example

Arrays

array=( $(ls) )

array=( $(echo ${x/*>/}) )

EXAMPLE:

array=( $(*) )


OTHER USEFUL COMMANDS

strings, top, htop, ps

s=$(osascript -e 'choose folder with prompt "Please choose a file"')

s=$(osascript -e 'choose file with prompt "Please choose a file"')

s=$(osascript -e 'tell app "System Events" to display dialog "Are you sure you like Kittens?"')
